{
  "builders": [
    {
      "type": "amazon-ebs",
      "region": "eu-west-1",
      "source_ami": "ami-776a659d",
      "instance_type": "t2.nano",
      "ssh_username": "ec2-user",
      "ami_name": "nodejs 0.0.1 {{timestamp}}"
    }
  ],
  "provisioners": [
    {
      "type": "file",
      "source": "files/etc/nginx/conf.d/nginx.conf",
      "destination": "/tmp/nginx.conf"
    },
    {
      "type": "shell",
      "execute_command": "echo 'packer' | sudo -S sh -c '{{ .Vars }} {{ .Path }}'",
      "inline": [
        "curl --silent --location https://rpm.nodesource.com/setup_8.x | bash -",
        "curl --silent -o epel.rpm https://dl.fedoraproject.org/pub/epel/epel-release-latest-7.noarch.rpm",
        "yum install -y ./epel.rpm",
        "yum install -y nodejs",
        "amazon-linux-extras install nginx1.12",
        "mv /tmp/nginx.conf /etc/nginx/nginx.conf",
        "systemctl enable nginx"
      ]
    }
  ]
}
